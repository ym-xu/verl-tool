---
description: Sync with upstream TIGER-AI-Lab/verl-tool
---
!`git fetch upstream`
!`git log --oneline HEAD..upstream/main | head -20`

Show what's new in upstream and merge if there are updates:

```
git merge upstream/main
```
